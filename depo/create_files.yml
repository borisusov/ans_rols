---
- name: Create file1
  copy:
      dest: /home/tomcat/folder1/file1.txt
      content: |
        Text Line1, in file1
        Text Line2, {{ mytext }}

  delegate_to: tomcat

- name: Create file2
  copy:
      dest: /home/jenkins/folder2/file2
      content: |
        Text Line1, in file2
        Text Line2, {{ mytext }}

  delegate_to: jenkins     
